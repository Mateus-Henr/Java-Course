package tutorial;

public class Main
{
    // JUnit
    // It's a unit testing framework used write tests that will run automate fashion. When working with teams, it's
    // important, so if a hundred percent of the tests are successful means that the code that he has written/modified
    // is great to go.
    //
    // You can either add JUnit by adding a "Library" to the project, or you can just place the cursor over the class
    // name, in our case, we will be using the "BankAccount" class, after placing the cursor press "Alt" + "Enter" and
    // click on "Create Test", and press "OK". After that you just need to configure JUnit as you will.
    public static void main(String[] args)
    {
        System.out.println("The application is running");
    }

}
